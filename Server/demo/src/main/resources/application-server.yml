spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://pre20.caprut5wigfx.ap-northeast-2.rds.amazonaws.com:3306/seb_40_pre_020?serverTimezone=Asia/Seoul
    username: ${PRE_DB_USERNAME}
    password: ${PRE_DB_PASSWORD}
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

    hibernate:
      ddl-auto: none  # (1) 스키마 자동 생성
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true      # (2) SQL 쿼리 출력
    properties:
      hibernate:
        format_sql: true  # (3) SQL pretty print
jwt:
  secret-key: ${JWT_SKY}
  access-token-expiration-seconds: 7200
